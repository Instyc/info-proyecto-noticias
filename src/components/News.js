import { Card, Typography } from "@mui/material";
import React from "react";

export const News = () => {
  return (
    <Card>
      <Typography variant="h4">www.web.com</Typography>
      <Typography variant="h2">Titulo</Typography>
      <img src="" alt="" />
      <Typography variant="muted">Publicado el: </Typography>
    </Card>
  );
};
